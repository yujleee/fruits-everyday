import AvailableItems from './AvailableItems';
import Summary from '../Layout/Summary';

const Fruits = () => {
  return (
    <div>
      <Summary />
      <AvailableItems />
    </div>
  );
};

export default Fruits;
